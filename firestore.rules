rules_version = '2';
service cloud.firestore {
  match /databases/guestbook/{document} {
    allow read, write:
        if request.auth.uid != null
  }
  match /databases/place/{document} {
    allow read:
        if true
    allow write:
        if resource.data.ownerId == request.auth.uid
  }
}